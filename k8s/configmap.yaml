apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-api-config
  namespace: todo-api
data:
  PORT: "3000"
  NODE_ENV: "production"
  AWS_REGION: "us-east-1"
  DYNAMODB_TABLE_NAME: "todos"
  USE_MEMORY_STORE: "false"